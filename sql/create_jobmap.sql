/* DROP TABLE jobmap;*/
CREATE TABLE jobmap(
	jk text PRIMARY KEY,
	efccid text,
	srcid text,
	cmpid text,
	srcname text,
	cmp text,
	cmpesc text,
	cmplnk text,
	loc text,
	country text,
	zip text,
	city text,
	title text,
	locid text,
	rd text);
